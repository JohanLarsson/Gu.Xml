using System;
using System.Linq.Expressions;
using System.Reflection;

namespace Gu.Xml
{
    public static class Field
    {
        //public static void Set<T>(this Expression<Func<T>> field, T value)
        //{
        //    var expression = (MemberExpression)field.Body;
        //    var item = ((ConstantExpression)expression.Expression).Value;
        //    var fieldInfo = (FieldInfo)expression.Member;
        //    fieldInfo.SetValue(item, value);
        //}
    }
}
